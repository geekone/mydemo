import 'package:flutter/material.dart';



class WinArticlesStart extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("文章列表准备抓取的"),),
      body:Center(
        child: Text("....."),
      )
    );
  }
}